<h1>This Page displays our bookings list</h1>

<h2>All of the following should be contained in individual booking cards</h2>

<ul>
<% @bookings.each do |booking| %>
<li>the chef's name is: <%= booking.chef.name %>, the customer's name is: <%= booking.customer.name %>, this will be a meal for <%= booking.number_of_guests %> persons, the booking's status is: <%= booking.status %>

  <ul>
    <% if !booking.messages.empty? %>
      <% booking.messages.each do |message| %>
      <li><%= message.content %></li>
      <% end %>
    <% end %>
  </ul>

<% end %>
</ul>

